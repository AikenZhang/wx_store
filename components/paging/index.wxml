<scroll-view enable-back-to-top scroll-y bindscrolltolower="loadData" class='fy-page'>
  <slot></slot>
  <load-more type="load" wx:if="{{_loadImg}}"></load-more>
  <load-more type="end" wx:if="{{_end}}"></load-more>
  <empty wx:if="{{_empty}}"></empty>
</scroll-view>